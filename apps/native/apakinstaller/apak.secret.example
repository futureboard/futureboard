# Legacy encrypted APAK v1 only.
# Copy to .apak.secret and pass --secret-file explicitly.
# New APAK v2 packages use signed.key and require no user-side secret.
APAK_SECRET=replace-with-a-random-local-secret
